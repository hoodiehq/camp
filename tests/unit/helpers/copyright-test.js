import { copyright } from 'camp/helpers/copyright';
import { module, test } from 'qunit';

module('Unit | Helper | copyright');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = copyright([42]);
  assert.ok(result);
});
